// @flow

import { css } from 'glamor';

export const styles = {
  container: css({
    border: '1px solid gray',
    borderRadius: '3px',
    padding: '1px 5px',
    display: 'inline-block',
  }),
  dib: css({ display: 'inline-block' })
};
